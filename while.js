const nomeEstudantes = ['joao', 'maria', 'jose', 'fernando', 'lucas', 'andre'];

let indice = 0;

while(indice < nomeEstudantes.length){ //enquanto a variavel indice for menor que o tamanho da variavel (5)
    console.log(nomeEstudantes[indice]);
    indice++;
}