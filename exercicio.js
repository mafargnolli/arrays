//criando um array de 5 numeros

const numeros = [0, 3, 20, 30, 18];

let i = 0;

//enquanto i for menor do que o tamanho do array
while(i < numeros.length){
    //executar isso
    console.log(numeros[i]);
    i++;
}